[package]
name = "unnamed"
version = "0.1.0"
edition = "2024"
autobins = false

[lib]
path = "src/main.rs"
crate-type = ["staticlib"]
test = false
bench = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"